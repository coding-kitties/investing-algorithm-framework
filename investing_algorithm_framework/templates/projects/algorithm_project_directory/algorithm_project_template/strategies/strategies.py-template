from investing_algorithm_framework.templates.strategies \
    import RelationalStrategy

from {{ project_name }}.data_providers.data_providers import MyDataProvider
from {{ project_name }}.configuration.extensions import logger

"""
Define here all your strategies
"""


class MyStrategy(RelationalStrategy):
    id = 'my_strategy'
    run_after = MyDataProvider

    def apply_strategy(self) -> None:
        logger.info("Running Strategy: {}".format(self.get_id()))
